# API + WS use relative URLs — works from localhost AND any LAN IP.
# Do NOT set NEXT_PUBLIC_API_URL or NEXT_PUBLIC_WS_URL here unless
# you need a fixed external domain (production).
# Next.js rewrites (:3000) and nginx (:3001) both proxy /api/* and /ws/* to the backend.
